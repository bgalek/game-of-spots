import React from 'react';

import repositories from '../api/chats';

export default React.createClass({
    displayName: 'App',

    render() {
        return (
            <div className="app">
                <h1>App</h1>
            </div>
        );
    }
});
