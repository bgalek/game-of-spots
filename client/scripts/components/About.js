import React from 'react';

export default React.createClass({
    displayName: 'About',
    render() {
        return (
            <div>
                about page
            </div>
        );
    }
});
